# 配额管理操作说明

## 概述

配额管理功能用于限制和控制组织内不同维度（账号、部门、成员）的 Token 消费额度。通过灵活的配额设置，帮助企业更好地管理成本和资源分配。

---

## 核心概念

### 价格计算标准
- **配额展示价格** = Token 消费单价（官网列表价格）× 配额消费折扣
- 此价格仅用于配额额度显示，不代表实际账单价格
- **账单结算价格** 以合同约定为准

### 重要说明
- 受结算周期影响，可能会出现使用量轻微超出配额的情况
- 修改配额后，配额限制立即生效

---

## 功能模块

### 1. 配额管理统一配置

用于全局配置预警规则和通知方式。

#### 设置项

| 配置项 | 说明 | 默认值 |
|--------|------|--------|
| **预警阈值** | 当配额使用率达到此百分比时触发预警 | 80% |
| **配额消费折扣** | 计算配额展示价格时应用的折扣系数 | 10折 |
| **预警消息接收人** | 接收预警通知的联系人 | 可选 |

#### 操作流程

1. **设置预警阈值**
   - 输入预警触发百分比（例：80 表示使用量达到 80% 时触发）
   - 阈值范围建议：50%-95%

2. **设置配额消费折扣**
   - 输入折扣系数（例：4.5 表示 4.5 折）
   - 此折扣用于计算配额展示价格

3. **配置预警接收人**
   - 点击"选择联系人"下拉框
   - 在弹出的人员列表中勾选需要接收通知的人员
   - 点击"保存设置"确认

4. **保存配置**
   - 所有项目配置完成后，点击"保存"按钮
   - 系统立即应用新配置

---

### 2. 账号维度配置

为整个账号设置 Token 消费总预算。

#### 功能说明

- **启用/禁用**：通过开关控制是否启用账号级配额限制
- **月度总预算**：设置账号在一个自然月内的最大消费额度（单位：元）
- **使用情况**：显示本月已使用金额和剩余预算

#### 操作流程

1. **启用账号配额管理**
   - 打开"启用账号配额管理"开关
   - 开关变为蓝色表示启用成功

2. **设置月度总预算**
   - 在"月度总预算"输入框输入预算金额
   - 只接受数字输入（单位：元）
   - 例：50000 表示月度预算 5 万元

3. **保存配置**
   - 点击"保存"按钮
   - 系统即时应用新预算设置

4. **监控使用情况**
   - 实时显示"本月使用"金额和使用百分比
   - 使用率达到预警阈值时会显示橙色警告

---

### 3. 部门维度配置

为组织内各部门设置独立的 Token 消费配额。

#### 部门结构

系统支持多级部门结构：
- **一级部门**：例如"技术中心、产品中心、市场中心"等
- **二级部门**：例如"研发一部、研发二部、架构部"等
- **三级部门**：例如"前端组、后端组"等

#### 操作流程

**添加部门配额：**

1. 点击"添加部门配置"按钮
2. 在弹出的对话框中进行以下操作：
   - **选择部门**：在部门树中展开并选择目标部门
     - 支持一级、二级、三级部门
     - 显示部门成员数量
   - **设置月度配额**：输入该部门的月度消费限额（单位：元）
   - 点击"添加"确认

**管理已有配额：**

1. 在部门配额表中查看所有已配置的部门
2. 表格显示的信息：
   - 部门名称（含全路径）
   - 部门成员数
   - 月度配额金额
   - 已使用金额
   - 使用率进度条

**编辑部门配额：**
- 点击操作列的编辑图标（铅笔）
- 修改配额金额
- 点击保存

**删除部门配额：**
- 点击操作列的删除图标（垃圾桶）
- 确认删除操作
- 删除后该部门将恢复为全账户配额管理

---

### 4. 成员维度配置

为组织内各成员设置个人的 Token 消费配额。

#### 功能说明

- **个人配额**：限制单个成员的月度 Token 消费额度
- **灵活管理**：支持不同成员设置不同的配额
- **实时监控**：显示每个成员的使用情况

#### 操作流程

**添加成员配额：**

1. 点击"添加成员配置"按钮
2. 在弹出的对话框中进行以下操作：
   - **选择成员**：
     - 在搜索框输入成员名称或邮箱快速查找
     - 从列表中选择目标成员
   - **设置月度配额**：输入该成员的月度消费限额（单位：元）
   - 点击"添加"确认

**查看成员配额：**

1. 成员配额表显示所有已配置的成员信息：
   - 成员名称和邮箱
   - 所属部门
   - 月度配额金额
   - 已使用金额
   - 使用率进度条

2. 使用搜索框快速查找特定成员：
   - 支持按成员名称搜索
   - 支持按邮箱地址搜索
   - 搜索实时生效

**编辑成员配额：**
- 点击操作列的编辑图标（铅笔）
- 修改配额金额
- 点击保存

**删除成员配额：**
- 点击操作列的删除图标（垃圾桶）
- 确认删除操作
- 删除后该成员将恢复为部门配额管理

---

## 配额层级关系

配额管理遵循以下优先级：
1. **成员级配额**（优先级最高）：如果成员有独立配额，则使用成员配额
2. **部门级配额**：成员无独立配额时，使用其所在部门的配额
3. **账号级配额**：部门和成员无配额时，使用整个账号的总预算

---

## 使用情况监控

### 使用率显示

- **进度条**：可视化显示消费进度
- **百分比标签**：显示使用占比
  - 绿色（<80%）：正常使用
  - 橙色（≥80%）：接近上限，需要注意

### 预警通知

当使用率达到预警阈值时：
- 系统自动通知已配置的接收人
- 提醒内容包含：部门/成员名称、当前使用率、剩余预算等

---

## 常见问题

### Q: 配额修改后何时生效？
A: 配额修改后立即生效，对新的 Token 消费计数生效。

### Q: 如果使用量超出配额会怎样？
A:
- 不会强制停止服务
- 出于结算周期等原因可能轻微超出
- 账单结算时按实际使用量和合同价格计费

### Q: 显示的配额价格和账单价格一样吗？
A:
- **配额价格** = 官网价格 × 折扣，仅用于额度展示
- **账单价格** 以合同约定的实际价格为准

### Q: 可以设置不同的折扣吗？
A: 目前折扣是全局统一设置，对所有配额都适用。

### Q: 删除部门配额后会怎样？
A:
- 该部门成员将不再受部门级配额限制
- 改为使用账号级配额或其个人配额

### Q: 能否为部分成员单独设置更严格的限额？
A: 可以，为成员设置的个人配额优先于部门配额，可设置更严格或更宽松的限制。

---

## 最佳实践

### 部门配额设置

1. **合理分配**：根据各部门的实际需求分配配额
2. **差异化管理**：高频使用部门可适当提高配额
3. **定期审查**：根据实际使用情况调整配额

### 成员配额设置

1. **按角色分配**：不同角色的成员设置不同配额
   - 项目经理：较高配额
   - 普通开发者：标准配额
   - 实习生：较低配额

2. **按项目分配**：关键项目成员可设置更高配额

### 预警配置

1. **预警阈值设置**：建议设置在 70%-80%
2. **通知人员选择**：
   - 管理员必须添加
   - 财务人员建议添加
   - 部门负责人可选添加

### 定期维护

1. 每月月底检查使用情况
2. 根据以下月份的使用需求调整配额
3. 及时清理已离职员工的配额设置
4. 定期检查部门配额是否合理

---

## 支持与反馈

如有任何关于配额管理的问题，请联系：
- **技术支持**：support@company.com
- **财务管理**：finance@company.com
- **系统管理员**：admin@company.com
